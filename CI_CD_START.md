# 🎉 CI/CD 配置完成指南

> **恭喜！** 你的项目已配置完整的 CI/CD 系统。本文档帮助你快速开始。

---

## 📋 完成的工作清单

### ✅ 已创建的文件

#### GitHub Actions 工作流
- [x] `.github/workflows/deploy.yml` - 自动化 CI/CD 流程

#### Docker 配置
- [x] `Dockerfile` - 多阶段 Docker 镜像构建
- [x] `docker-compose.yml` - 容器编排（API + Nginx）
- [x] `nginx.conf` - 反向代理和 HTTPS 配置

#### 脚本和工具
- [x] `scripts/deploy.sh` - 手动部署脚本
- [x] `scripts/server-init.sh` - 服务器一键初始化
- [x] `scripts/local-test.sh` - 本地容器测试
- [x] `scripts/setup-github-secrets.sh` - GitHub Secrets 配置

#### 配置文件
- [x] `.env.example` - 环境变量模板
- [x] `.gitignore` - Git 忽略规则更新

#### 文档
- [x] `CICD_GUIDE.md` - 详细部署指南（450+ 行）
- [x] `QUICKSTART.md` - 快速参考（300+ 行）
- [x] `CI_CD_FILES_SUMMARY.md` - 文件说明
- [x] `CI_CD_START.md` - 本文件

---

## 🚀 立即开始（5 分钟快速开始）

### 第 1 步：初始化服务器（一次性）

假设你的服务器信息：
- 地址：`123.45.67.89`
- 用户：`root`

```bash
# 在本地运行：复制初始化脚本到服务器
scp scripts/server-init.sh root@123.45.67.89:/tmp/

# SSH 连接到服务器运行
ssh root@123.45.67.89
sudo bash /tmp/server-init.sh
# 按提示完成配置（约 2-3 分钟）

# 验证完成
exit
```

**预期结果**：
- Docker ✓
- Docker Compose ✓
- deploy 用户已创建 ✓
- 防火墙已配置 ✓
- `/home/deploy/mortgage-agent` 目录已创建 ✓

### 第 2 步：测试本地 Docker

```bash
# 在本地项目目录运行
bash scripts/local-test.sh

# 预期输出：
# [SUCCESS] 所有测试通过！
# 
# 可访问：http://localhost:8000/docs
```

### 第 3 步：配置 GitHub Secrets

#### 方法 A：使用脚本（推荐）

```bash
# 前置条件：安装 GitHub CLI
brew install gh  # macOS

# 登录 GitHub
gh auth login

# 运行配置脚本
bash scripts/setup-github-secrets.sh

# 按提示输入：
# - Docker Hub 用户名
# - Docker Hub 密码/Token
# - 服务器 IP（123.45.67.89）
# - SSH 用户名（deploy）

# 脚本自动上传到 GitHub
```

#### 方法 B：手动配置

1. 打开 GitHub 仓库网页
2. 点击 `Settings` → `Secrets and variables` → `Actions`
3. 逐个添加以下 Secrets：

| Secret 名称 | 值 |
|-----------|-----|
| `DOCKER_USERNAME` | 你的 Docker Hub 用户名 |
| `DOCKER_PASSWORD` | 你的 Docker Hub Personal Access Token |
| `SERVER_USER` | `deploy` |
| `SERVER_HOST` | `123.45.67.89` |
| `SERVER_PORT` | `22` |
| `SERVER_PRIVATE_KEY` | **见下面的说明** |

**获取 SSH 私钥**：

```bash
# 在本地运行
cat ~/.ssh/id_rsa

# 输出示例（完整复制包括首尾）：
# -----BEGIN RSA PRIVATE KEY-----
# MIIEowIBAAKCAQEA1234567890...
# ...
# -----END RSA PRIVATE KEY-----
```

### 第 4 步：推送代码触发部署

```bash
# 在本地项目目录运行
git add .
git commit -m "chore: add CI/CD configuration"
git push origin main

# GitHub Actions 自动开始部署！
```

### 第 5 步：查看部署进度

打开 GitHub 仓库网页：
1. 点击 `Actions` 标签页
2. 查看 `CI/CD Pipeline` 工作流的执行进度
3. 等待所有步骤完成（通常 5-10 分钟）

**期望流程**：
```
✅ test (4-5 分钟)
  ├─ Checkout code
  ├─ Set up Python
  ├─ Code format check (Black)
  ├─ Import sort check (isort)
  ├─ Code quality check (Flake8)
  ├─ Run tests
  └─ Upload coverage

✅ build (3-5 分钟)
  ├─ Set up Docker Buildx
  ├─ Login to Docker
  ├─ Extract metadata
  └─ Build and push image

✅ deploy (5-10 分钟)
  ├─ Configure SSH
  ├─ Create deploy script
  ├─ Execute remote deploy
  └─ Verify deployment
```

---

## 📌 关键概念解释

### GitHub Actions 工作流

```
你的代码 → Git Push → GitHub Actions 自动执行
                     ├─ 运行测试（失败则停止）
                     ├─ 构建 Docker 镜像
                     ├─ 推送到 Docker Hub
                     └─ SSH 连接服务器部署
                        ├─ 拉取最新代码
                        ├─ 重启容器
                        └─ 健康检查
```

### Docker 容器结构

```
你的应用 → Docker 镜像（Dockerfile 定义）
        → Docker 容器（docker-compose.yml 启动）

容器编排：
┌─────────────────────────────────────┐
│      Docker Network Bridge           │
├─────────────────────────────────────┤
│                                     │
│  ┌──────────────────────────────┐  │
│  │  mortgage-api (FastAPI)      │  │
│  │  内部端口：8000              │  │
│  │  外部访问：仅 localhost      │  │
│  └──────────────────────────────┘  │
│           ↑        ↓                │
│      (通过内部网络)                  │
│           ↓        ↑                │
│  ┌──────────────────────────────┐  │
│  │  nginx (反向代理)             │  │
│  │  HTTP/HTTPS：0.0.0.0:80/443  │  │
│  │  外部访问：所有 IP            │  │
│  └──────────────────────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

### SSL/TLS 证书

```
开发环境：
  └─ 自签名证书（已自动生成）
     └─ 浏览器会显示"不安全"警告（正常）

生产环境：
  └─ Let's Encrypt 免费证书
     └─ 需要域名和 certbot 配置
     └─ 详见 CICD_GUIDE.md 的第 5 步
```

---

## 🔧 常用命令

### 查看部署日志

```bash
# 在服务器上查看 API 日志
ssh deploy@123.45.67.89
cd ~/mortgage-agent
docker-compose logs -f mortgage-api

# 查看 Nginx 日志
docker-compose logs -f nginx

# 查看容器状态
docker-compose ps
```

### 手动重启服务

```bash
# SSH 连接到服务器
ssh deploy@123.45.67.89
cd ~/mortgage-agent

# 重启所有容器
docker-compose restart

# 或仅重启 API
docker-compose restart mortgage-api
```

### 获取输出文件

```bash
# 获取生成的 PDF 报告
scp deploy@123.45.67.89:~/mortgage-agent/output/*.pdf ./

# 获取所有输出（使用递归）
scp -r deploy@123.45.67.89:~/mortgage-agent/output ./
```

### 回滚到上一版本

```bash
ssh deploy@123.45.67.89
cd ~/mortgage-agent

# 查看可用的备份
ls -la backups/

# 恢复备份
cp backups/docker-compose.yml.20240225_120000 docker-compose.yml
docker-compose down
docker-compose up -d
```

---

## 🐛 故障排查

### ❌ GitHub Actions 显示测试失败

**可能原因**：代码格式不符合要求

**解决**：
```bash
# 本地格式化代码
pip install black isort
black mortgage_agent/
isort mortgage_agent/

# 提交修复后重新推送
git add .
git commit -m "style: format code"
git push origin main
```

### ❌ Docker 镜像推送失败

**可能原因**：Docker Hub 凭证错误

**解决**：
```bash
# 本地测试 Docker 登录
docker login -u your_username

# 重新生成 Personal Access Token
# https://hub.docker.com/settings/security

# 更新 GitHub Secrets 中的 DOCKER_PASSWORD
```

### ❌ SSH 连接超时

**可能原因**：防火墙未配置或 SSH 密钥不对

**解决**：
```bash
# 验证本地 SSH 连接
ssh -i ~/.ssh/id_rsa -p 22 deploy@123.45.67.89

# 检查服务器防火墙
ssh root@123.45.67.89 "ufw status"

# 如果防火墙未配置，运行
ssh root@123.45.67.89 "bash /tmp/server-init.sh"
```

### ❌ 应用无法访问

**可能原因**：容器未成功启动

**解决**：
```bash
ssh deploy@123.45.67.89
cd ~/mortgage-agent

# 查看容器日志
docker-compose logs

# 检查容器状态
docker-compose ps

# 强制重启
docker-compose down
docker-compose up -d

# 等待容器启动
sleep 10

# 测试 API
curl http://localhost:8000/health
```

---

## 📚 更多资源

| 文档 | 用途 |
|-----|------|
| `QUICKSTART.md` | 快速参考（推荐阅读） |
| `CICD_GUIDE.md` | 详细部署指南 |
| `CI_CD_FILES_SUMMARY.md` | 所有文件说明 |
| README.md | 项目说明 |

---

## ✨ 部署验证

部署完成后，验证以下内容：

```bash
# 1. 检查应用是否在线
curl https://123.45.67.89/health

# 2. 访问 Swagger 文档
# 浏览器打开：https://123.45.67.89/docs

# 3. 测试 API
curl -X POST https://123.45.67.89/v1/mortgages/prepayment:calc \
  -H "Content-Type: application/json" \
  -d '{
    "principal": 1000000,
    "annual_rate": 3.5,
    "term_months": 360,
    "method": "equal_payment",
    "paid_periods": 24,
    "prepay_amount": 100000
  }'

# 4. 检查容器运行状态
ssh deploy@123.45.67.89 "cd ~/mortgage-agent && docker-compose ps"
```

**预期结果**：
- ✅ 健康检查返回 `{"status": "ok"}`
- ✅ Swagger 文档可以访问
- ✅ API 返回计算结果
- ✅ 两个容器都在运行

---

## 🎯 下一步

### 短期（本周）

- [ ] 完成初次部署
- [ ] 验证应用正常运行
- [ ] 配置域名解析（可选）
- [ ] 配置 Let's Encrypt 证书（可选）

### 中期（本月）

- [ ] 添加单元测试
- [ ] 设置日志监控
- [ ] 配置告警通知
- [ ] 制定备份策略

### 长期（持续优化）

- [ ] 性能调优
- [ ] 负载均衡配置
- [ ] CI/CD 流程细化
- [ ] 生产监控面板

---

## 🙋 常见问题

**Q: 我的服务器是 CentOS，行吗？**
A: 可以。`server-init.sh` 是针对 Ubuntu 的，CentOS 需要修改包管理器命令（apt → yum）

**Q: 如何修改应用端口？**
A: 编辑 `docker-compose.yml` 的 `ports` 字段，例如：`"8080:8000"`

**Q: 可以多个服务器部署吗？**
A: 可以。复制 `docker-compose.yml` 到多台服务器，或使用 Docker Swarm/Kubernetes

**Q: 如何自动续期 SSL 证书？**
A: 详见 `CICD_GUIDE.md` 的"Let's Encrypt 自动续期"部分

**Q: GitHub Actions 有使用限制吗？**
A: 免费账户有 2000 分钟/月 的限制（公开仓库无限制）

---

## 📞 需要帮助？

1. **查看脚本输出** - 脚本会输出详细的错误信息
2. **查看日志** - `docker-compose logs` 显示容器日志
3. **查看完整文档** - `CICD_GUIDE.md` 有详细说明
4. **GitHub Actions 日志** - 点击失败的工作流查看详细日志

---

## 🎊 恭喜！

你现在拥有一个完整的 CI/CD 系统：

✅ 自动化测试  
✅ 自动化构建  
✅ 自动化部署  
✅ 零停机发布  
✅ 自动健康检查  
✅ HTTPS 支持  
✅ 日志管理  
✅ 轻松回滚  

**开始推送代码吧！** 🚀

```bash
git add .
git commit -m "chore: add CI/CD configuration"
git push origin main
```

---

**版本**：1.0  
**更新日期**：2026-02-25  
**下一个学习点**：阅读 `CICD_GUIDE.md` 了解详细内容

